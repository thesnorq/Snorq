version: "3.9"

services:
  snorq:
    build: .
    container_name: snorq-skill
    volumes:
      - .:/app
    command: python -m pytest tests/ -v

  dashboard:
    image: node:20-alpine
    container_name: snorq-dashboard
    working_dir: /app
    volumes:
      - ./dashboard:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
